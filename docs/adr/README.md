# ADRs

This folder is reserved for **Architecture Decision Records** (ADRs).

When a change has meaningful trade-offs (auth model, prompt storage, analytics schema, deployment posture),
capture it here as a short markdown note so future changes have context.

Recommended naming:

- `0001-decision-title.md`
- `0002-decision-title.md`

